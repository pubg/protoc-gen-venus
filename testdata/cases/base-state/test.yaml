name: "Base Component: State"
description: "Test for State field"
inputFiles: ["entry.proto"]
expectResultFiles: ["entry.venus.json"]
expectResultIsNull: false

expectedBehaviorDescription: |
  if state not exist:
    do nothing
  
  if state exist:
    set state to provided
  
  if state is unspecified:
    do nothing

